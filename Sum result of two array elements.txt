//Sum result of two array elements

#include <stdio.h>

int main()
{
     
     //array1
     
      int row,line,i,j,k,n;
      
      printf("Enter the number of row:");
      scanf("%d",&row);
      
      printf("Enter the number of line:");
      scanf("%d",&line);
      
      int array1 [row][line];
      
      for(i=0;i<row;i++)
      {
          for(j=0;j<line;j++){
              printf("[%d][%d] ----> ",i+1,j+1);
              scanf("%d",&array1[i][j]);
          }
      }
      
       printf("Composed array 1:\n");
       
      for(k=0;k<row;k++)
      {
          for(n=0;n<line;n++)
          {
              printf("%d ",array1[k][n]);
          }
        printf("\n");
      }
      
     
       //array2      
            
       int l,m,p,r;
      
      printf("Enter the number of row:");
      scanf("%d",&row);
      
      printf("Enter the number of line:");
      scanf("%d",&line);
      
      int array2 [row][line];
      
      for(l=0;l<row;l++)
      {
          for(m=0;m<line;m++){
              printf("[%d][%d] ----> ",l+1,m+1);
              scanf("%d",&array2[l][m]);
          }
      }
      
       printf("Composed array 2:\n");
       
      for(p=0;p<row;p++)         
      {
          for(r=0;r<line;r++)
          {
              printf("%d ",array2[p][r]);
          }
        printf("\n");
      } 
      
      //sum
        
       printf("Sum result of two array elements:\n");
       
       int sum [row][line];
       int x,y;
       
       for(x=0;x<row;x++)
       {
           for(y=0;y<line;y++)
           {
              sum[x][y]=array1[x][y]+array2[x][y];
           }
       }
       
        int v,z;
        
        for(v=0;v<row;v++)
        {
            for(z=0;z<line;z++)
            {
                printf("%d ",sum[v][z]);
            }
            printf("\n");
        }
          